
## Album shuffle mode

- Time sorting and map location should be optional and independently activated through game settings
- Review scoring. Both time sorting and map location should each be optional and independently scored. Each task give a total of SCORE_MAX_POINTS points (rounded up)
- Create a leaderboard for each game mode. Add each game mode setting as a column
- Dropdown for selecting map pin should be removed. Pin selection should be done strictly be clicking on the pin on the map.
- Make images in sorting tab 50% bigger and make tab just as big as necessary. The rest of  the space occupy with the map.
- WHen selecting a photo in the list, if its assigned to a map icon, the icon should make clear in some way (maybe by highlighting it)
- The map should offer full screen option like the other game mode
- "See final results" is not working; also it is being shown during round reveal which is wrong. It should come only after the last round.
- Round reveal is not showing all users.

- Leaderboard CSV schema should be checked on startup. If schema has changed in new version, do something about it.
- 0,0 coordinates should be skipped
- in Album shuffle mode, I need a solution for pins in the same space. SHould they be avoided?
- IN album shuffle mode, only pictures
